# VulnSpiral
 full-featured fuzzing framework with a modern web UI for testing networked DUTs (like Wi-Fi CPEs).   It supports seed-based fuzzing, interface selection, TCP/UDP, PCAP capture, and full integration with ITSAR fuzzing evidence workflows.
